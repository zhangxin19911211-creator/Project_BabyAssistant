---
description: Prefer WeChat Mini Program official components, APIs, and extended capabilities before custom or third-party solutions.
globs: miniprogram/**/*,cloudfunctions/**/*
alwaysApply: true
---

# 微信小程序官方能力优先

后续在本仓库做小微信程序/云开发相关开发时：

1. **组件** 优先查并用 [组件文档](https://developers.weixin.qq.com/miniprogram/dev/component/)：视图容器（如 `scroll-view`、`swiper`、`page-container`）、基础内容、表单（`button`、`input`、`textarea`、`picker` 等）、导航、媒体、画布等；Skyline 下另有 `draggable-sheet` 等扩展布局能力。
2. **API** 优先查并用 [API 文档](https://developers.weixin.qq.com/miniprogram/dev/api/)：路由（`switchTab`、`navigateTo` 等）、界面交互、网络、存储、媒体（含 `RecorderManager` 录音）、开放接口（登录、授权、隐私）、WXML 节点查询等；实现前先 `wx.canIUse` 或查文档确认基础库与端能力。
3. **扩展能力** 优先查并用 [平台能力 · 拓展能力](https://developers.weixin.qq.com/miniprogram/dev/platform-capabilities/extended/)：WeUI / 扩展组件库、**微信同声传译**（语音输入等）、自然语言理解、OCR 等；需插件时在小程序后台添加插件并在 `app.json` 声明，再按插件文档接入。

原则：能用官方组件/API/插件解决的，避免重复造轮子；文档版本与基础库要求以官方页面为准。
